# http://travis-ci.org/#!/Carreau/IPConfig
virtualenv: 
    system_site_packages: true 
before_install: 
    - sudo apt-get install -qq python-numpy
language: python
python:
    - 2.7
    - 3.3
install:
    - python setup.py install 
script:
    - nosetests
